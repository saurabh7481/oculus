{
  "name": "oculus",
  "version": "0.1.0",
  "private": true,
  "description": "Realtime multiuser collaboration and syncing SDK.",
  "type": "module",
  "packageManager": "pnpm@10.16.1",
  "scripts": {
    "dev": "pnpm -r --parallel --if-present dev",
    "build": "pnpm --filter @oculus/sdk build && pnpm --filter @oculus/svelte build && pnpm --filter @oculus/server build && pnpm --filter @oculus/demo build",
    "test": "vitest run",
    "typecheck": "tsc -b packages/server packages/sdk packages/svelte apps/demo",
    "server": "pnpm --filter @oculus/server dev",
    "demo": "pnpm --filter @oculus/demo dev"
  },
  "devDependencies": {
    "@types/bun": "^1.2.23",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vite": "^6.0.3",
    "vitest": "^2.1.8"
  }
}
